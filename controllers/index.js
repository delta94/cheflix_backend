const userController = require('./users');
const classController = require('./classes');
const materialController = require('./materials');

module.exports = {
    userController,
    classController,
    materialController
}